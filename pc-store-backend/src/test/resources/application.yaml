spring:
  datasource:
    url: jdbc:h2:mem:pc-store-h2-database-test;DB_CLOSE_ON_EXIT=false
  jpa:
    defer-datasource-initialization: true
  sql:
    init:
      data-locations: classpath:/global-variables.sql

app:
  archiver:
    scheduling:
      enable: false