import {AutoTrimDirective} from './auto-trim.directive';

describe('AutoTrimDirective', () => {
  it('should create an instance', () => {
    // const directive = new AutoTrimDirective();
    // expect(directive).toBeTruthy();
  });
});
