spring:
  datasource:
    url: jdbc:h2:mem:pc-store-h2-database-test;DB_CLOSE_ON_EXIT=false
  jpa:
    defer-datasource-initialization: true

app:
  archiver:
    scheduling:
      enable: false

jwk-set-uri: http://localhost:8180/realms/master/protocol/openid-connect/certs